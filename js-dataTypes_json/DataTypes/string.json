[
    {
        "component": "header",
        "variant": "2",
        "value": "Strings<br>"
    },  
    {
        "component": "text", 
        "value": "<b>Example:</b><br>"
    },
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "var greet1 = 'Hi Nayak!';  // using single quotes;<br>"
    },
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "/var greet2 = \"Hi Nayak!\";  // using double quotes<br>"
    },
    {
        "component": "text", 
        "value": "In the above example the variable a contains a greeting which is saying<br>"
    },
    {
        "component": "text", 
        "value": "<b>Hi Nayak</b><br>"
    },
    {
        "component": "text", 
        "value": ", if you see the saying that is actually a<br>"
    },
    {
        "component": "text", 
        "value": "<b>string</b><br>"
    },
    {
        "component": "text", 
        "value": "The string data type is used to represent textual data (i.e. sequences of characters). Strings are created using 'single' or \"double quotes\" surrounding one or more characters, as shown below:<br>"
    },
    {
        "component": "text", 
        "value": "var name='kumar'<br>console.log(name);<br>output : kumar<br>"
    },
    {
        "component": "text", 
        "value": "output : kumar<br>"
    },
    {
        "component": "text", 
        "value": "var name=\"kumar\"<br>console.log(name);<br>"
    },
    {
        "component": "text", 
        "value": "output : kumar<br>"
    },
    {
        "component": "text", 
        "value": "You can include quotes inside the string as long as they don't match the enclosing quotes.<br>"
    },
    {
        "component": "text", 
        "value": "<b>Example:</b><br>"
    },
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "var a = \"I’m going to the classroom\"; // single quote inside double quotes <br>"
    },
    {
        "component": "text", 
        "value": "If you observe the above example, in variable a we have a sentence that is I’m going to the classroom, in this sentence if you see a single quote is there after I ,it won’t create a problem that is not matching with enclosing quotes because enclosing quotes are double quotes.<br>"
    },
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "var b = 'He said \"bye\" and went out.';  // double quotes inside single quotes<br>"
    },
    {
        "component": "text", 
        "value": "If you observe the above example, in variable b we have a sentence that is He said \"bye\" and went out.  In this sentence if you see a double quote for bye inside a single quote  ,it won’t create a problem that is not matching with enclosing quotes because enclosing quotes are double quotes.<br>"
    },
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "var c = 'We'll never leave you.';     // escaping single quote with backslash<br>"
    },
    {
        "component": "text", 
        "value": "If you observe the above example, if we have single quotes inside single quotes then it will create a problem for you to resolve that issue you need to use an escape sequence.<br>"
    }
]
