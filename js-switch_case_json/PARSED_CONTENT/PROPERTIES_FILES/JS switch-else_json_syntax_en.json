{
	"syntax_key1": "<span style=\"\">Syntax</span>",
	"syntax_key2": "How default case works",
	"syntax_key3": "Example:",
	"syntax_key4": "Explanation",
	"syntax_key5": "Here the program will take user <span style=\"\">input</span> ,this <span style=\"\">statement</span> is very first;",
	"syntax_key6": "Then it move to the switch <span style=\"\">statement</span> where it is mentioned switch (<span style=\"\">true</span>);",
	"syntax_key7": "Then it  moves to the case 1 that is num is greater than zero and if that mentioned case is not similar to the case so it will directly jump to the case 2.",
	"syntax_key8": "Here in case 2 it will again check whether num is less than zero, is not similar to num above so it will directly jump to the case 3.",
	"syntax_key9": "Here is case 3 it will again check whether num is equal to zero is not similar to num which is user <span style=\"\">input</span>, so it will directly jump to the default case value and console \"invalid <span style=\"\">input</span>\"."
}