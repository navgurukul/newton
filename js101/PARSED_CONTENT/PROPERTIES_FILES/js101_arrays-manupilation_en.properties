arrays-manupilation_key1=name: Arrays mein cheezon ko daalte aur nikalte kaise hain?
arrays-manupilation_key2=Explain arrays operation such as push, pop, splice
var taskList = ['Teach students', 'Cook Food', 'Study', 'Give interview'];
//push
taskList.push('Be honest');
var listLength = taskList.length;
var index = 0;
while(index < listLength)
      console.log(taskList[i]);
      index = index + 1;
}



arrays-manupilation_key3=//pop
var task = taskList.pop();
console.log(task);
arrays-manupilation_key4=//delete say 3rd element
taskList.splice(0, 1)
listLength = taskList.length;
index = 0;
while(index < listLength)
        console.log(taskList[i]);
        index = index + 1;
}
