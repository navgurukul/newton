arrays-iteration_key1=name: Baar baar dekho! Hazaar baar dekho
arrays-iteration_key2=Explain the concept of traversing and accessing elements in a list


arrays-iteration_key3=//Concept of arrays and iteration over them
var taskList = ['Teach students', 'Cook Food', 'Study', 'Give interview'];
arrays-iteration_key4=var listLength = taskList.length;
var index = 0;
while(index < listLength)
        console.log(taskList[i]);
        index = index + 1;
}
