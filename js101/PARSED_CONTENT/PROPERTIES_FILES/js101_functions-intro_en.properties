functions-intro_key1=name: Ek baar karo! Anek baar karvao. Functions ka dhamaal
functions-intro_key2=Convert the print list to a function and perform different operations on list and print this list


functions-intro_key3=function printList(listToPrint) {
        var listLength = listToPrint.length;
        var index = 0;
        while(index < listLength)
            console.log(listToPrint[i]);
            index = index + 1;
        }
}
functions-intro_key4=var taskList = ['Teach students', 'Cook Food', 'Study', 'Give interview'];
taskList.push('Be honest');
printList(taskList)
functions-intro_key5=taskList.splice(0, 1)
printList(taskList)
