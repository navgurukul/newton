[
    {
        "component": "code",
        "type": "javascript",
        "title" : "will add later",
        "value": "let isTrue = \"True\";<br>if(isTrue){<br>&emsp;console.log(\"Output should show\")<br>}<br>if(isTrue === \"False\"){<br>&emsp;console.log(\"Both are same\")<br>}<br>else {<br>&emsp;console.log(\"Please run it. if all conditions false\")<br>}<br>}"
    },
    {
        "component": "text",
        "value": "Output should show <br>Please run it. if all conditions false"
    }
]
