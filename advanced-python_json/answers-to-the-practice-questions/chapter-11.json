[
	{
		"component": "header",
		"variant": 1,
		"value": "Chapter 11"
	},
	{
		"component": "text",
		"value": "The webbrowser module has an open() method that will launch a web browser to a specific URL, and that’s it. The requests module can download <span style=\"\">files</span> and pages from the Web. The BeautifulSoup module parses HTML. Finally, the selenium module can launch and control a browser."
	},
	{
		"component": "text",
		"value": "The requests.get() function returns a Response object, which has a text attribute that contains the downloaded content as a <span style=\"\">string</span>."
	},
	{
		"component": "text",
		"value": "The raise<i>for</i>status() method raises an exception if the download had problems and does nothing if the download succeeded."
	},
	{
		"component": "text",
		"value": "The status_<span style=\"\">code</span> attribute of the Response object contains the HTTP status <span style=\"\">code</span>."
	},
	{
		"component": "text",
		"value": "After opening the new <span style=\"\">file</span> on your computer in 'wb' “write binary” mode, use a for <span style=\"\">loop</span> that iterates over the Response object’s iter_content() method to write out chunks to the <span style=\"\">file</span>. Here’s an example:"
	},
	{
		"component": "text",
		"value": "<span style=\"\">save</span><span style=\"\">File</span> = open('<span style=\"\">file</span>name.html', 'wb')<br>for chunk in res.iter_content(100000):<br>    <span style=\"\">save</span><span style=\"\">File</span>.write(chunk)<br>F12 brings up the developer tools in Chrome. Pressing CTRL-SHIFT-C (on Windows and Linux) or ⌘-OPTION-C (on OS X) brings up the developer tools in Firefox."
	},
	{
		"component": "text",
		"value": "Right-click the element in the page, and select Inspect Element from the menu."
	},
	{
		"component": "text",
		"value": "'#main'"
	},
	{
		"component": "text",
		"value": "'.highlight'"
	},
	{
		"component": "text",
		"value": "'div div'"
	},
	{
		"component": "text",
		"value": "'button[value=\"favorite\"]'"
	},
	{
		"component": "text",
		"value": "spam.getText()"
	},
	{
		"component": "text",
		"value": "linkElem.attrs"
	},
	{
		"component": "text",
		"value": "The selenium module is imported with from selenium import webdriver."
	},
	{
		"component": "text",
		"value": "The find<i>element</i>* methods return the first matching element as a WebElement object. The find<i>elements</i>* methods return a list of all matching elements as WebElement objects."
	},
	{
		"component": "text",
		"value": "The click() and send_<span style=\"\">keys</span>() methods simulate mouse clicks and <span style=\"\">key</span>board <span style=\"\">keys</span>, respectively."
	},
	{
		"component": "text",
		"value": "Calling the submit() method on any element within a form submits the form."
	},
	{
		"component": "text",
		"value": "The forward(), back(), and refresh() WebDriver object methods simulate these browser buttons."
	}
]